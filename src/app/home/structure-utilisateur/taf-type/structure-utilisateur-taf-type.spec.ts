import { StructureUtilisateurTafType } from './structure-utilisateur-taf-type';

describe('StructureUtilisateurTafType', () => {
  it('should create an instance', () => {
    expect(new StructureUtilisateurTafType()).toBeTruthy();
  });
});
