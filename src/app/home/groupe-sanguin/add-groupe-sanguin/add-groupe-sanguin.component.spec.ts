import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AddGroupeSanguinComponent } from './add-groupe-sanguin.component';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting } from '@angular/common/http/testing';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

describe('AddGroupeSanguinComponent', () => {
  let component: AddGroupeSanguinComponent;
  let fixture: ComponentFixture<AddGroupeSanguinComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
       providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        NgbActiveModal
      ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AddGroupeSanguinComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
