import { FicheEnfantTafType } from './fiche-enfant-taf-type';

describe('FicheEnfantTafType', () => {
  it('should create an instance', () => {
    expect(new FicheEnfantTafType()).toBeTruthy();
  });
});
