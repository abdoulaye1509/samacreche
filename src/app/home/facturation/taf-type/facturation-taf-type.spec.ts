import { FacturationTafType } from './facturation-taf-type';

describe('FacturationTafType', () => {
  it('should create an instance', () => {
    expect(new FacturationTafType()).toBeTruthy();
  });
});
