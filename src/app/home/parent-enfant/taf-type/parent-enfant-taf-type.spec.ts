import { ParentEnfantTafType } from './parent-enfant-taf-type';

describe('ParentEnfantTafType', () => {
  it('should create an instance', () => {
    expect(new ParentEnfantTafType()).toBeTruthy();
  });
});
