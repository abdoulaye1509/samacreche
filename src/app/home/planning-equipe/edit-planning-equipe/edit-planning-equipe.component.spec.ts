import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditPlanningEquipeComponent } from './edit-planning-equipe.component';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting } from '@angular/common/http/testing';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

describe('EditPlanningEquipeComponent', () => {
  let component: EditPlanningEquipeComponent;
  let fixture: ComponentFixture<EditPlanningEquipeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
       providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        NgbActiveModal
      ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(EditPlanningEquipeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
