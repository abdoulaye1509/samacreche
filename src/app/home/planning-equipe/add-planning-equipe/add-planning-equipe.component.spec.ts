import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AddPlanningEquipeComponent } from './add-planning-equipe.component';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting } from '@angular/common/http/testing';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

describe('AddPlanningEquipeComponent', () => {
  let component: AddPlanningEquipeComponent;
  let fixture: ComponentFixture<AddPlanningEquipeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
       providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        NgbActiveModal
      ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AddPlanningEquipeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
